# Local real-club setup only. See docs/DEPLOYMENT.md for an HTTPS deployment.
APP_ENV=development
COOKIE_SECURE=false
# Generate each value with: openssl rand -hex 32
REAL_JWT_SECRET=
POSTGRES_PASSWORD=
